import { CAllDirectivesDirective } from './c-all-directives.directive';

describe('CAllDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new CAllDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
